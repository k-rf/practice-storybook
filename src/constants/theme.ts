import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    background: {
      paper: 'linear-gradient(to right, #ef32d9, #89fffd)',
      default: 'linear-gradient(to right, #ef32d9, #89fffd)',
    },
  },
});
